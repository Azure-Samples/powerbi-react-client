import React, { Component } from 'react'

/* global powerbi */
export class FilterPane extends Component {
  constructor(props) {
    super(props);
    this.state = {
    };
  }

  render() {
    return (
      <div>Filter Pane Placeholder (Not Implemented)</div>
    )
  }
}

export default FilterPane;